{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": ["../base.tsconfig.no-dom.json", "./bundler.tsconfig.json"],
  "compilerOptions": {
    /* If your code doesn't run in the DOM: */
  }
}

